<!DOCTYPE html>
<html>
  <head>
    <%= render "layouts/head_assets" %>
  </head>

  <body class="bg-gray-100">
  <%= render "layouts/flash_messages" %>

  <main style="padding-bottom: calc(24px + env(safe-area-inset-bottom, 0px));">
    <%= yield %>
  </main>
  </body>
</html>
<%= render "layouts/flash_dismiss_script" %>

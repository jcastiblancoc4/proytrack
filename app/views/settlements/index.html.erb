<div class="max-w-7xl mx-auto px-3 sm:px-4 mt-4">
  <div class="mb-6">
    <!-- Header -->
    <div class="flex flex-col sm:flex-row sm:justify-between sm:items-center mb-6 gap-3">
      <div>
        <h1 class="text-xl sm:text-2xl font-extrabold text-blue-900">💰 Liquidaciones</h1>
        <p class="text-xs sm:text-sm text-gray-600 mt-1">
          Gestión de liquidaciones mensuales
        </p>
      </div>

      <div class="flex flex-col sm:flex-row gap-2">
        <%= link_to "Nueva Liquidación", new_settlement_path, class: "w-full sm:w-auto inline-flex items-center justify-center gap-2 px-4 py-2 bg-blue-600 text-white text-sm font-semibold rounded hover:bg-blue-700 transition-colors duration-200 shadow-sm" %>
      </div>
    </div>

    <!-- Sección de Preliquidación del Mes Actual -->
    <div class="mb-8 bg-white rounded-xl shadow-md overflow-hidden">
      <!-- Header -->
      <div class="bg-gradient-to-r from-purple-50 to-blue-50 border-b border-purple-100 px-4 py-3">
        <div class="flex items-center justify-between">
          <h2 class="text-lg font-bold text-gray-800">
            📊 Preliquidación <%= @month_name.capitalize %> <%= @current_year %>
          </h2>
          <%= link_to "Ver Detalle →", preliquidation_settlements_path, class: "text-xs font-medium text-purple-700 bg-purple-100 hover:bg-purple-200 px-3 py-1.5 rounded-lg transition-colors duration-200" %>
        </div>
      </div>

      <!-- Grid de valores -->
      <div class="p-4">
        <div class="grid grid-cols-1 md:grid-cols-3 gap-3">
          <!-- Total Proyectos -->
          <div class="bg-gradient-to-br from-green-50 to-green-100 rounded-lg p-4 shadow-sm hover:shadow-md transition-shadow duration-200">
            <div class="flex items-center justify-between mb-1.5">
              <h3 class="text-xs font-semibold text-green-800">Proyectos Terminados</h3>
              <span class="text-xl">✅</span>
            </div>
            <p class="text-2xl font-bold text-green-700 mb-0.5">
              <%= number_to_currency(@preliq_total_projects, unit: "$", delimiter: ".", separator: ",", precision: 0) %>
            </p>
            <p class="text-xs font-medium text-green-600">
              <%= @preliq_projects_count %> <%= @preliq_projects_count == 1 ? 'proyecto' : 'proyectos' %>
            </p>
          </div>

          <!-- Total Gastos -->
          <div class="bg-gradient-to-br from-red-50 to-red-100 rounded-lg p-4 shadow-sm hover:shadow-md transition-shadow duration-200">
            <div class="flex items-center justify-between mb-1.5">
              <h3 class="text-xs font-semibold text-red-800">Gastos del Mes</h3>
              <span class="text-xl">💸</span>
            </div>
            <p class="text-2xl font-bold text-red-700 mb-0.5">
              <%= number_to_currency(@preliq_total_expenses, unit: "$", delimiter: ".", separator: ",", precision: 0) %>
            </p>
            <p class="text-xs font-medium text-red-600">
              <%= @preliq_expenses_count %> <%= @preliq_expenses_count == 1 ? 'gasto' : 'gastos' %>
            </p>
          </div>

          <!-- Diferencia -->
          <div class="bg-gradient-to-br <%= @preliq_difference >= 0 ? 'from-blue-50 to-blue-100' : 'from-orange-50 to-orange-100' %> rounded-lg p-4 shadow-sm hover:shadow-md transition-shadow duration-200">
            <div class="flex items-center justify-between mb-1.5">
              <h3 class="text-xs font-semibold <%= @preliq_difference >= 0 ? 'text-blue-800' : 'text-orange-800' %>">Diferencia</h3>
              <span class="text-xl"><%= @preliq_difference >= 0 ? '💰' : '⚠️' %></span>
            </div>
            <p class="text-2xl font-bold <%= @preliq_difference >= 0 ? 'text-blue-700' : 'text-orange-700' %> mb-0.5">
              <%= number_to_currency(@preliq_difference, unit: "$", delimiter: ".", separator: ",", precision: 0) %>
            </p>
            <p class="text-xs font-medium <%= @preliq_difference >= 0 ? 'text-blue-600' : 'text-orange-600' %>">
              <%= @preliq_difference >= 0 ? '✓ Superávit' : '✗ Déficit' %>
            </p>
          </div>
        </div>

        <!-- Nota informativa -->
        <div class="mt-3 flex items-start gap-2 bg-purple-50 rounded-lg p-3">
          <svg class="w-4 h-4 text-purple-600 flex-shrink-0 mt-0.5" fill="currentColor" viewBox="0 0 20 20">
            <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd"></path>
          </svg>
          <div class="flex-1">
            <p class="text-xs text-purple-800">
              <strong>Nota:</strong> Esta es una vista previa con proyectos "Terminados" y gastos "Pendientes". Para crear la liquidación oficial, usa "Nueva Liquidación".
            </p>
          </div>
        </div>
      </div>
    </div>

    <!-- Lista de Liquidaciones Creadas -->
    <div>
      <h2 class="text-lg sm:text-xl font-bold text-gray-800 mb-4">📋 Liquidaciones Creadas</h2>

    <% if @settlements.any? %>
      <!-- Lista de Liquidaciones -->
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
        <% @settlements.each do |settlement| %>
          <div class="bg-white rounded-lg shadow-md border border-gray-200 p-6 hover:shadow-lg transition-shadow duration-200">
            <!-- Header de la tarjeta -->
            <div class="flex items-center justify-between mb-4">
              <h3 class="text-lg font-bold text-gray-800">
                📅 <%= settlement.period_name %>
              </h3>
              <% if settlement.user == current_user %>
                <a href="#" onclick="if(confirm('¿Estás seguro de eliminar esta liquidación? Los proyectos y gastos volverán a estar disponibles.')) { document.getElementById('delete-settlement-<%= settlement.id %>').submit(); } return false;" class="text-red-600 hover:text-red-800">
                  <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"></path>
                  </svg>
                </a>
                <%= form_with url: settlement_path(settlement), method: :delete, local: true, html: { id: "delete-settlement-#{settlement.id}", style: "display: none;" } do |f| %>
                <% end %>
              <% end %>
            </div>

            <!-- Información de la liquidación -->
            <div class="space-y-3">
              <div class="flex justify-between items-center">
                <span class="text-sm text-gray-600">Total Proyectos:</span>
                <span class="text-sm font-semibold text-green-600">
                  <%= number_to_currency(settlement.total_projects_value, unit: "$", delimiter: ".", separator: ",", precision: 0) %>
                </span>
              </div>

              <div class="flex justify-between items-center">
                <span class="text-sm text-gray-600">Total Gastos:</span>
                <span class="text-sm font-semibold text-red-600">
                  <%= number_to_currency(settlement.total_expenses_value, unit: "$", delimiter: ".", separator: ",", precision: 0) %>
                </span>
              </div>

              <div class="pt-3 border-t border-gray-200">
                <div class="flex justify-between items-center">
                  <span class="text-sm font-bold text-gray-700">Diferencia:</span>
                  <span class="text-base font-bold <%= settlement.difference >= 0 ? 'text-green-600' : 'text-red-600' %>">
                    <%= number_to_currency(settlement.difference, unit: "$", delimiter: ".", separator: ",", precision: 0) %>
                  </span>
                </div>
              </div>

              <div class="text-xs text-gray-500 mt-3">
                Creada el <%= settlement.created_at.in_time_zone("Bogota").strftime("%d/%m/%Y %I:%M %p") %>
              </div>
            </div>

            <!-- Acciones -->
            <div class="mt-4">
              <%= link_to "Ver Detalles", settlement_path(settlement), class: "w-full inline-block text-center px-4 py-2 bg-blue-100 text-blue-700 text-sm font-medium rounded hover:bg-blue-200 transition-colors" %>
            </div>
          </div>
        <% end %>
      </div>
    <% else %>
      <!-- Estado vacío -->
      <div class="text-center py-12">
        <h3 class="mt-2 text-sm font-medium text-gray-900">No hay liquidaciones</h3>
        <p class="mt-1 text-sm text-gray-500">Comienza creando tu primera liquidación mensual.</p>
        <div class="mt-6">
          <%= link_to "Nueva Liquidación", new_settlement_path, class: "inline-flex items-center px-4 py-2 bg-blue-600 text-white text-sm font-medium rounded hover:bg-blue-700" %>
        </div>
      </div>
    <% end %>
    </div>
  </div>
</div>
